server.cfgver=20191205
server.id = 
server.name = Server 1

server.gds-type = PURE_JAVA

server.host = localhost
server.port = 3050
server.trusted-auth = false
server.sysdba-login = SYSDBA
server.sysdba-password = masterkey

server.default-directory = ${agent.default-directory}/${server.id}

server.installation =
server.bin = ${server.installation}/bin
server.log = ${server.installation}/firebird.log
server.config = ${server.installation}/firebird.conf
server.aliases = ${server.installation}/aliases.conf
