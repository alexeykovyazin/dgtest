#enabled = false == for text purposes this field will always be overwritted
enabled = false

#will be overwrietten
time_threshold = 100

#critical values
print_plan = true
print_perf = true
max_sql_length = 65000
max_arg_length = 500
max_arg_count = 500
max_log_size = 0

#to reduce file size
log_sweep = false
log_connections = false
log_transactions = false
log_initfini = false

#this is must be true value:
log_statement_finish = true


#if empty string then will be skipped
#include_filter =
#exclude_filter =
